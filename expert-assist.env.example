# Expert Assist — environment variables (reference)
# Copy values into .env.local (do not commit secrets).

# Twilio SMS — REST uses API Key (recommended)
TWILIO_ACCOUNT_SID=
TWILIO_API_KEY_SID=
TWILIO_API_KEY_SECRET=

# Webhook signature validation uses your **primary Account Auth Token** from Twilio Console
# (Twilio does not sign webhooks with the API key secret). Prefer explicit name:
TWILIO_WEBHOOK_AUTH_TOKEN=
# Legacy REST (Account SID + Auth Token only — prefer API keys above)
# TWILIO_AUTH_TOKEN=

# Messaging Service SID from Twilio Console (must start with MG, not AC).
TWILIO_MESSAGING_SERVICE_SID=
# If unset or invalid, falls back to TWILIO_FROM_NUMBER (must be MMS-capable for photos).
# TWILIO_FROM_NUMBER=+1...

# Public base URL Twilio uses for webhooks (must match Messaging config)
TWILIO_WEBHOOK_BASE_URL=https://your-domain.com

# MMS — configure the same Messaging Service / number in Twilio Console:
# - Inbound webhook: POST {TWILIO_WEBHOOK_BASE_URL}/api/twilio/inbound-sms
#   (downloads MediaUrl0…N into Supabase bucket consult-media)
# - Status callback: POST {TWILIO_WEBHOOK_BASE_URL}/api/twilio/status
# Outbound MMS uses signed consult-media URLs as Twilio mediaUrl (expert composer attach).

# Optional copy overrides
# EXPERT_ASSIST_COPY_WELCOME=
# EXPERT_ASSIST_HELP_TEXT=

# Stripe (card on file + charges)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Vercel cron shared secret (?token=)
CRON_CONSULTS_TOKEN=

# Public Expert Assist app (tesla-expert-assist/surfaces on Vercel) — intake at / and shop surfaces at /s/<token>
# Use the same base URL for both unless you split deployments intentionally.
EXPERT_ASSIST_SURFACES_PUBLIC_URL=
# Public /s/<locationId> paths use the shop UUID (no JWT). Optional legacy secret unused by invite links.
# EXPERT_ASSIST_SHOP_JWT_SECRET=

# Customer consult intake (/?shop=<location_uuid>) — usually same host as SURFACES_PUBLIC_URL
EXPERT_ASSIST_INTAKE_PUBLIC_URL=

# Web intake form (POST /api/expert-assist/intake) — Bearer auth + optional CORS.
# Generate a long random string; same value is embedded in the static intake app config.
EXPERT_ASSIST_INTAKE_SECRET=
# Comma-separated origins allowed to call the intake API from a browser (e.g. https://your-intake.netlify.app).
# Leave unset for same-origin only (no CORS headers).
EXPERT_ASSIST_INTAKE_ALLOWED_ORIGINS=

# Zoho Lens — expert-initiated live video on open consults (reuse Zoho OAuth below)
# OAuth refresh token must include scope: ZohoLens.sessionapi.CREATE
# ZOHO_CLIENT_ID=
# ZOHO_CLIENT_SECRET=
# ZOHO_REFRESH_TOKEN=
ZOHO_LENS_DEPARTMENT_ID=
# ZOHO_LENS_ASSIGNEE_ZUID=
# ZOHO_LENS_API_BASE=https://lens.zoho.com/api/v2
# ZOHO_LENS_WEB_HOST=lens.zoho.com
# EXPERT_ASSIST_LENS_DEFAULT_TIMEZONE=America/Los_Angeles

# Slack (new cases, approvals, and idle-queue nudges from cron — no expert SMS)
# EXPERT_ASSIST_SLACK_WEBHOOK_URL=
# CRM consult deep links in Slack (defaults to NEXTAUTH_URL, then https://crm.fixlane.app)
# CRM_PUBLIC_BASE_URL=https://crm.fixlane.app
# Resend (receipts / failures) — reuse existing CRM RESEND_API_KEY
# RESEND_API_KEY=
# RESEND_FROM=
